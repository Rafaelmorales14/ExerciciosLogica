programa {
  funcao inicio() 
  {
    real base, h, area
    escreva("Digite o comprimento da base de um triângulo: ")
    leia(base)
    escreva("Digite a altura desse triângulo: ")
    leia(h)
    area = (base * h) / 2
    escreva("A área do triângulo digitado é ", area)
  }
}
